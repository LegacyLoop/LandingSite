# LANDING MASTER-ARC — SEQUENCED WAVE QUEUE
# CMD-LANDING-MASTER-ARC V21 decomposed into one-at-a-time waves per CEO ruling (2026-08-05 PM):
# "do the price fix tightly, then sequence everything else so we flow one to the next — yes/no at
#  the end of each chat." Every wave ships to the Apple / SpaceX / $150k / Awwwards bar or it iterates.
# NO EMOJI. Author: Agent A. Anchor after FIX 5 ships: see git log for the pricing commit SHA.

## DONE
- FIX 5 · PRICING RESTORED + ELEVATED — SHIPPED this chat. Founding tiers ($0/$10/$25/$75 +
  12/8/5/4% commission, traced to WaitlistWalkthrough OFFERINGS), founding-100 framing, working
  CTAs into the waitlist, zero emoji, glow/stagger, Barlow Condensed numbers. WaitlistWalkthrough
  aligned (dropped "Dedicated account manager" + "API access"; "White-label" -> "Branded store").
- FIX 7 VERIFICATION (pre-done, truth gate) — all six engines confirmed in the app stack, ready to
  cite when FIX 7 fires: OpenAI/Claude/Gemini/Grok in lib/adapters/multi-ai.ts (207/262/361/448);
  DeepSeek in lib/sylvia/triage-router.ts:71 + litellm_config.yaml; Perplexity in litellm_config.yaml
  + app/api/bots/*/route.ts. Council = the 4 voting engines. R-3 is true — the SIX-engine claim can ship.

## THE QUEUE (fire order — each its own chat, yes/no to advance)

### WAVE 1 · FIX 1 — DEEP RECON + NARRATIVE ARC  (GATE — unblocks the hero)
Scope: /designlang:extract on 3-5 living references (Apple product page · Awwwards-SOTY · Stripe/Linear
· a premium product-tour) + R-5 depth (pull real HTML/CSS/JS, catalog scroll choreography, easing,
type scale, grid, video treatment). Two docs: LANDING_DESIGN_BEAT_SHEET + LANDING_NARRATIVE_ARC
(tail ruled R-8: ESTATE -> JOIN -> DOWNLOAD -> MISSION -> FOOTER). No page code ships.
Size: M (research + 2 docs). Dep: none. Gate: both docs on disk, every downstream FIX mapped to a beat.

### WAVE 2 · FIX 2 — MEDIA VERDICTS (ffmpeg frame review)
Scope: ffmpeg frame-extract the 4 CLIP videos + arc-called assets; claims-vs-CANONICAL_FACTS table;
verdicts USE/USABLE/DO-NOT-USE (kills PENDING-CEO-VISUAL). Update the manifest.
Size: S-M. Dep: WAVE 1 (arc says which assets matter). Gate: zero PENDING rows, claims table complete.

### WAVE 3 · FIX 3 — HERO REPOSITION  (push hardest)
Scope: order 1 BuyerBot moat · 2 end-to-end loop · 3 Message Center. Beat-sheet techniques ->
frontend-design + huashu variants (MIN 2 iteration rounds) -> ux-copy every sentence -> build inline,
preserve preloader/Lenis/iPad/WAAPI + working animations -> design-critique -> designlang:grade vs
benchmark, iterate until grade >= benchmark or HALT with the gap named. Ten root videos still resolve.
Size: L (the flagship design lift). Dep: WAVE 1 (gate). Gate: tsc/build/truth-gate + grade >= benchmark.

### WAVE 4 · FIX 4 — CINEMATIC VIDEO SPINE
Scope: wire verdicted clips per arc beats (loop spine first): masked/device frames, scroll-tied reveals,
poster crossfades, scrims, in-view play/pause. reduced-motion = static posters. LCP < 2.5s ABSOLUTE,
lazy below fold, never autoplay-with-sound. Stage only referenced assets (scoped adds).
Size: M-L. Dep: WAVE 2 (verdicts) + WAVE 3 (hero/loop structure). Gate: build + LCP evidence + no stall.

### WAVE 5 · FIX 6 — ESTATE STORY REBUILD  (downsizing-first, R-2)
Scope: reframe headline/intro to RETIREMENT + DOWNSIZING lead (bereavement = one compassionate lane,
never the headline). Each offering (White-Glove · Estate Care · Neighborhood Bundle) gets a visualized,
animated story BEFORE its CTA. Keep: consult-first, ZERO estate prices, intake DARK, "within one
business day", senior floors. Size: M-L. Dep: WAVE 1 (arc), verdicted estate media (WAVE 2) helps.
Gate: downsizing-lead headline, three story sequences, CTA after story, truth-gate PASS.

### WAVE 6 · FIX 7 — HEXA-AI SECTION  (verification already done)
Scope: upgrade the AI section to a SIX-engine constellation (OpenAI/Claude/Gemini/Grok/DeepSeek/
Perplexity) powering app + Sylvia-AI, with the MegaBot 4-AI COUNCIL intact as its own feature ("Four
AIs. One consensus price."). Per-provider SVG marks + colors (canon: OpenAI #22c55e · Claude #a78bfa ·
Gemini #3b82f6 · Grok #f97316; pick tasteful DeepSeek + Perplexity colors, record in beat sheet).
Page-wide AI-count coherence (6 engines / 4 council — never two contradictory numbers). Cite the
verified file:lines (see DONE above). Size: L. Dep: WAVE 1. Gate: coherence grep + grade >= benchmark.

### WAVE 7 · FIX 8 — AI-TELL PURGE  (65 glyphs -> custom SVG system, R-4)
Scope: every pictograph used as UI/iconography across app/landing/* + app/estate/* -> a refined inline-SVG
icon system (single-weight 1.5px stroke, 24px grid, brand-teal; estate = estate-warm gold; senior-legible).
Functional glyphs (close/menu/arrows) may stand. Hype-word sweep on edited surfaces.
Size: L (page-wide, ~65 sites). Dep: ideally after the section rebuilds (3/5/6) so we do not re-purge
rebuilt sections. Gate: pictograph grep (U+1F300-1FAFF, U+2600-27BF) = 0.

### WAVE 8 · FIX 9 — END-TO-END TIGHTEN + REORDER  (R-7 + R-8) — LAST
Scope: reorder the render so the tail = ESTATE -> JOIN -> DOWNLOAD -> MISSION -> FOOTER (move JOIN
directly under estate, then DOWNLOAD, then MISSION at bottom). Reorder, do not rewrite; preserve every
animation; re-choreograph entrance triggers + the estate-gold -> join-teal seam. Full coherence pass:
transitions, spacing rhythm, PURPOSE-pillar cut of anything not earning its frame budget, 375px walk.
Size: M. Dep: LAST (all sections final). Gate: playwright DOM-order assertion + full-page walk clean.

## NOTES
- WAVE 8 (reorder) goes LAST so it moves final sections, not moving targets.
- WAVE 7 (emoji purge) is most efficient after 3/5/6 rebuild their sections (rebuilt sections ship
  emoji-free already — FIX 5 set the precedent: its section is zero-pictograph).
- Every wave: tsc 0 · build PASS · truth-gate PASS · playwright 375px · /pr-review-toolkit HARD GATE
  · git-sourced deploy · #34 by content · §12.
- Estate side stays consult-first / zero prices throughout (R-1). App repo READ-ONLY.

# END · one wave at a time, each at the bar · Connecting Generations.
